# Contributors run `pnpm install` on this public repo. Block lifecycle scripts
# from dependencies by default so a malicious transitive postinstall can't run.
# pnpm 11 already gates build scripts behind an allowlist; this makes it explicit.
ignore-scripts=true
